<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions needed for wallet operations -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to draw over other apps (for overlay UI) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Seed Vault Permissions -->
    <uses-permission android:name="com.solanamobile.seedvault.ACCESS_SEED_VAULT" />
    <uses-permission android:name="com.solanamobile.seedvault.ACCESS_SEED_VAULT_PRIVILEGED" />

    <application
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="false"
        android:name="com.michaeltchuang.walletsdk.demo.AndroidApp"
        tools:replace="android:name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name="com.michaeltchuang.walletsdk.demo.AppActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan"
            android:supportsPictureInPicture="true"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Overlay Activity for wallet UI (launched by CLIENT apps) -->
        <activity
            android:name="com.michaeltchuang.walletsdk.demo.service.ui.WalletOverlayActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Translucent"
            android:taskAffinity=""
            android:excludeFromRecents="true">
            <!-- Allow client apps to launch this activity -->
            <intent-filter>
                <action android:name="com.michaeltchuang.walletsdk.demo.service.WALLET_UI" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Bound Service that other apps can connect to -->
        <service
            android:name="com.michaeltchuang.walletsdk.demo.service.AlgoKitWalletService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="com.michaeltchuang.walletsdk.demo.service.WALLET_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- Override PasskeyProviderService attributes from wallet-sdk-ui library -->
        <service
            android:name="com.michaeltchuang.walletsdk.ui.passkeys.PasskeyProviderService"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name">
            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/provider" />
        </service>
    </application>
</manifest>
